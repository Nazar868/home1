# File Organizer CLI

Node.js CLI tool that helps analyze, organize and clean directories.

## Features

- Scan directories and generate file statistics
- Detect duplicate files using SHA-256 hashing
- Organize files by category
- Remove old files with dry-run safety

## Installation

```bash
git clone https://github.com/Nazar/file-organizer
cd file-organizer
npm install
